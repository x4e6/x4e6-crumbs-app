# crumbs module

Сейчас это заглушка (кнопка “collect crumbs” → `Gather...` + анимированный голубь).

Позже можно:

- добавить `crumbs.html` + `crumbs.js` в эту папку
- оставить маршрут `#/crumbs` прежним (без изменений в `src/app/routes.js`)

